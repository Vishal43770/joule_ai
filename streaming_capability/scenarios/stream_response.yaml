description: Answer questions, provide information, or trigger file upload component for PDF, DOCX, JPG, PNG files.

slots:
  - name: topic
    description: Topic or query for response generation
  - name: stream_format
    description: Data format (json or text)
  - name: action_mode
    description: Action mode (upload or stream)

target:
  type: function
  name: stream_response_function

response_context:
  - value: $target_result.aggregated_response
    description: Complete response content
  - value: $target_result.status
    description: Status of the action
